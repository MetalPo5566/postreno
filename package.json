{
  "name": "kleaner-postreno",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "description": "Kleaner post-renovation cleaning and formaldehyde removal page for postreno.kleaner.my",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/gen-owner-confirm.mjs",
    "preview": "astro preview",
    "assets": "node scripts/copy-fonts.mjs && node scripts/gen-logo.mjs && node scripts/gen-images.mjs && node scripts/gen-og.mjs",
    "verify": "node scripts/verify.mjs",
    "links": "node scripts/check-links.mjs",
    "qa": "node scripts/qa.mjs",
    "check": "npm run build && npm run verify",
    "lighthouse": "node scripts/lighthouse.mjs",
    "preview:bundle": "npm run build && node scripts/gen-preview.mjs",
    "logo": "node scripts/gen-logo.mjs"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@fontsource/lato": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "lighthouse": "^13.5.0",
    "playwright": "^1.56.0",
    "sharp": "^0.35.4",
    "tailwindcss": "^4.3.3"
  }
}
